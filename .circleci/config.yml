version: 2.1

orbs:
  node: circleci/node@3.0.0

jobs:
  build:
    executor: node/default
    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run test:coverage
      - run: npm run codecov

workflows:
  version: 2
  build:
    jobs:
      - build
